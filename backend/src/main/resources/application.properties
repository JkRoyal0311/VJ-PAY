spring.application.name=VJ Pay

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/paymentdb}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:root}

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
jwt.secret=${JWT_SECRET:MyVeryStrongSecretKeyThatIsAtLeast32Characters!}

logging.level.root=INFO
logging.file.name=logs/app.log

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:vjpayapp@gmail.com}
spring.mail.password=${MAIL_PASSWORD:gytwdwtojbiihikm}
# spring.mail.username=gurikerijayakrishna0311@gmail.com
# spring.mail.password=zonufkrnxovowvbs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration